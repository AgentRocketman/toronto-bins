Build a simple URL shortener web app. Users paste a long URL into a textbox and click Shorten. The app generates a 6-character random short code (alphanumeric), stores the mapping, and shows the short URL. Visiting the short URL redirects to the original. Include basic stats (click count per short URL). Use vanilla HTML/CSS/JS + PHP backend + JSON file for storage. Mobile responsive.
