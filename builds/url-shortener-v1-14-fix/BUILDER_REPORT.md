# BUILDER REPORT — v1.14 Quick Fix

**Change Request:** Add the text "hi how are you" below the time

## Files Modified

1. **index.html** — Added `#timeDisplay` and `#greeting` divs to footer
2. **assets/js/app.js** — Added `updateTime()` function to display current time, updates every second
3. **assets/css/style.css** — Added styles for `#timeDisplay` and `#greeting`

## Summary

Added time display at the bottom of the page with current date/time updating every second, followed by the text "hi how are you" as requested.
