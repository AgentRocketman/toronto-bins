# QUICK FIX — SURGICAL CHANGE

**Change:** put the  date and time at the bottom of the page

## Rules (MANDATORY — token efficiency matters)
1. Read ONLY the files you need to change. Do NOT read the whole codebase.
2. Use `grep` to locate the EXACT lines that need editing before opening files.
3. Edit ONLY the minimum files required for THIS change. Touching extra files = wasted tokens.
4. NO refactoring. NO 'improvements'. NO new features. Just the change requested.
5. Skip README, BUILDER_REPORT, or other docs unless the change is about docs.
6. After editing, write a SHORT BUILDER_REPORT.md — max 5 lines, just list files changed.

Existing code is in ./output/. Start with `ls output/` then `grep` for relevant terms.