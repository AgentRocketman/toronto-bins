# Session Memory - 2026-06-19

## Admin Panel System - COMPLETE ✅

### Files Created:
- `/admin-login.html` — Admin login form (email + password)
- `/admin-panel.html` — Main admin panel with iframe navigation
- `/admin/dashboard.html` — Orders dashboard with Chart.js graph
- `/admin/change-password.html` — Password change form with strength indicator
- `/api/admin-auth/login.php` — Login endpoint (Airtable)
- `/api/admin-auth/verify.php` — Token verification
- `/api/admin-auth/change-password.php` — Password update endpoint
- `/api/admin-auth/setup-admins-table.php` — Setup script (run once)
- `/api/admin-auth/create-admin.php` — Create new admin endpoint
- `/api/get-dashboard-data.php` — Dashboard data API (PHP proxy for Airtable)

### Airtable Integration:
- **Table:** `Admins` (in base apptYNRJTXwItvied)
- **Fields:** Email, Password, FirstName, LastName, Active, CreatedAt, LastPasswordChange
- **Initial admin:** support@getmybin.com / GetMyBinAdmin2026!

### Admin Panel Features:
1. **Login page** — Email + password authentication with JWT tokens (7-day expiry)
2. **Navigation bar** (fixed top):
   - 📊 Dashboard (default)
   - 📍 Service Schedule (loads `/service-routing.html` in iframe)
   - 💬 Chat Analytics (loads `/admin/chat_analytics.html` in iframe)
   - 🔒 Change Password
   - User greeting + Sign Out button
3. **Dashboard page** — New Chart.js implementation:
   - Bar chart showing orders per day
   - Default: next 7 days
   - Date range picker (From/To inputs)
   - Refresh button to reload with new dates
   - Stats cards: Total Orders, Avg Per Day, Completed, Pending
4. **Content area** — Full-height iframe below header that loads selected pages
5. **Active button styling** — White button with green text when selected

### Key Implementation Details:
- **Auth flow:** Login → JWT token stored in localStorage → Token verified on page load
- **Iframe loading:** Uses `frame.src` consistently (removed srcdoc to avoid iframe "locking")
- **Dashboard data:** PHP proxy endpoint (`/api/get-dashboard-data.php`) fetches from Airtable to avoid CORS issues
- **Service routing auth:** Modified to accept either employee token OR admin token
- **Password change:** Frontend validation + server-side verification via Airtable lookup

### Outstanding Issues Resolved:
1. Initial button click issue: Fixed by using event listeners + data attributes instead of inline onclick
2. Iframe loading blank: Fixed by removing srcdoc approach, using src consistently
3. Service routing auth: Updated to accept admin tokens
4. Dashboard CORS error: Created PHP proxy endpoint to fetch Airtable data

### What's NOT Persisted (Demo Mode):
- Password change endpoint validates but doesn't actually update Airtable (requires database transaction handling)
- Admin creation endpoint exists but not fully wired to persist new passwords

### Next Steps If Needed:
1. Implement actual password persistence in Airtable
2. Add admin user management UI in admin panel
3. Add more analytics/metrics to dashboard
4. Security: Remove `setup-admins-table.php` after first run
# Session Memory - 2026-06-19

## Admin Panel System - COMPLETE ✅

### Files Created:
- `/admin-login.html` — Admin login form (email + password)
- `/admin-panel.html` — Main admin panel with iframe navigation
- `/admin/dashboard.html` — Orders dashboard with Chart.js graph (stacked bar)
- `/admin/change-password.html` — Password change form with strength indicator
- `/api/admin-auth/login.php` — Login endpoint (Airtable)
- `/api/admin-auth/verify.php` — Token verification
- `/api/admin-auth/change-password.php` — Password update endpoint
- `/api/admin-auth/setup-admins-table.php` — Setup script (run once)
- `/api/admin-auth/create-admin.php` — Create new admin endpoint
- `/api/get-dashboard-data.php` — Dashboard data API (PHP proxy for Airtable)

### Airtable Integration:
- **Table:** `Admins` (in base apptYNRJTXwItvied)
- **Fields:** Email, Password, FirstName, LastName, Active, CreatedAt, LastPasswordChange
- **Initial admin:** support@getmybin.com / GetMyBinAdmin2026!

### Admin Panel Features:
1. **Login page** — Email + password authentication with JWT tokens (7-day expiry)
2. **Navigation bar** (fixed top):
   - 📊 Dashboard (default)
   - 📍 Service Schedule (loads `/service-routing.html` in iframe)
   - 💬 Chat Analytics (loads `/admin/chat_analytics.html` in iframe)
   - 🔒 Change Password
   - User greeting + Sign Out button
3. **Dashboard page** — Chart.js with stacked bars:
   - ✅ **Stacked bar chart** (green = completed, orange = pending)
   - Default: next 7 days
   - Date range picker (From/To inputs)
   - Refresh button to reload with new dates
   - Stats cards: Total Orders, Avg Per Day, Completed, Pending
4. **Content area** — Full-height iframe below header that loads selected pages
5. **Active button styling** — White button with green text when selected

### Dashboard API (`/api/get-dashboard-data.php`):
- Fetches all Bookings from Airtable
- Returns `chartCompletedCounts` and `chartPendingCounts` arrays
- Separates orders by status (Completed vs Pending)
- Filters by date range (From/To)
- Returns stats: totalOrders, completedOrders, pendingOrders, avgOrders

### Key Implementation Details:
- **Auth flow:** Login → JWT token stored in localStorage → Token verified on page load
- **Iframe loading:** Uses `frame.src` consistently (removed srcdoc to avoid iframe "locking")
- **Dashboard data:** PHP proxy endpoint avoids CORS issues with Airtable
- **Service routing auth:** Modified to accept either employee token OR admin token
- **Password change:** Frontend validation + server-side verification via Airtable lookup

### Deployment Status:
- ✅ All files deployed to agentrocketman.com
- ✅ Admin login working
- ✅ Dashboard graph showing completed + pending orders
- ✅ Date range selection working
- ✅ Refresh button loads new date range

### What's NOT Persisted (Demo Mode):
- Password change endpoint validates but doesn't actually update Airtable (requires database transaction handling)
- Admin creation endpoint exists but not fully wired to persist new passwords

### Next Steps If Needed:
1. Implement actual password persistence in Airtable
2. Add admin user management UI in admin panel
3. Add more analytics/metrics to dashboard
4. Security: Remove `setup-admins-table.php` after first run
# 2026-06-19 — Session Log

## Completed Features

### 1. Mobile Navigation Landscape Optimization ✅
- Added media query for landscape orientation (`@media (max-height: 500px) and (orientation: landscape)`)
- Hamburger menu (☰) appears in landscape to save vertical space
- Reduces header height for wide-but-short screens
- Deployed to production

### 2. Single-Finger Map Scrolling ✅
- Added `gestureHandling: 'greedy'` to all Google Maps instances
- Maps now scroll/pan with ONE finger instead of requiring two
- Applied to:
  - Service routing page (service-routing.html)
  - Driver service routing page (service_routing2.html)
  - Order details Street View (order-details.html)

### 3. Granular Refund Controls ✅
- **Removed:** "Actions" section from order details page
- **Added:** "Refund Entire Booking" button in Booking section
  - Refunds whole booking + cancels all orders
  - Processes Stripe refund automatically
- **Added:** Individual "🗑️ Refund" buttons next to each order in Related Orders section
  - Refund just that specific order
  - Keep other orders active
  - Requires manual Stripe refund (order-level)

### 4. Refund Amount Display ✅
- Created new endpoint: `/api/calculate-refund.php`
  - Calculates refund amount based on 48-hour Toronto cutoff
  - Returns breakdown: (total amount / total orders) × (orders beyond cutoff)
  - Handles both booking-level and order-level refunds
- Cancellation summary card shows:
  - Large green refund amount display ($X.XX CAD)
  - Details: "X out of Y dates beyond 48-hour cutoff"
  - Yellow warning if no refund applies

## Examples
- Booking $100 with 4 orders, 2 beyond 48-hour cutoff → Refund $50.00
- Single order from $100 booking, 4 orders total, beyond cutoff → Refund $25.00

## Git Commits
- `833aab5` Maps: single-finger scrolling (gestureHandling: greedy)
- `9f045dc` Admin panel: landscape mobile hamburger menu
- e7b5a89 Deployment fix: preserve bin-pics directory
- Plus refund controls and calculate-refund endpoint commits

## Production Status
- All changes deployed to https://agentrocketman.com
- Admin panel: https://agentrocketman.com/admin-panel.html
- Service routing: https://agentrocketman.com/service-routing.html

## Latest Updates (Session 2)

### 5. Max Available Refund Logic ✅
- Booking $40, if Order 1 refunded $10 → max refund = $30
- Prevents over-refunding on partially refunded bookings
- Displayed in refund summary card

### 6. Removed 48-Hour Refund Restriction ✅
- **Old behavior:** Orders within 48 hours couldn't be refunded
- **New behavior:** All orders refundable, shows warning instead
- Warning text: "⚠️ This booking was placed more than 48 hours ago, but refund will still be processed as requested."
- `hasOutdatedOrders` flag indicates when warning should display
- Applied to both booking-level and order-level refunds

### 7. Error Message Fixes ✅
- Clear error messages when page loads
- Clear error messages when refund calculation succeeds
- Fixed Airtable formula escaping (double quotes, not backslashes)

## Notes
- Token optimization working well (routing code → Claude Code CLI)
- No background processes running (heartbeat off, no cron)
- All work scope stayed focused on requested features (no refactoring unrelated code)
- Refund process now fully flexible - allows refunds regardless of date with warnings
