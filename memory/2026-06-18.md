# Session Notes — 2026-06-18

## Chat Analytics System — COMPLETE ✅

Built **comprehensive chat logging & analytics dashboard** for GetMyBin:

### **Backend Infrastructure**
- `/api/config.php` — Centralized config (Airtable keys, OpenAI key, helpers)
- `/api/log-chat.php` — Logs Q&A to Airtable ChatLogs table
- `/api/summarize-chats.php` — Queries Airtable, summarizes via OpenAI server-side
- `/api/chat.php` — **NEW** Handles chat messages securely (no exposed API key)

### **Frontend**
- `/chat-logger.js` — Captures sessionId, timestamps, IP, browser, device
- `/admin/chat_analytics.html` → **RENAMED TO** `/admin/chat_analytics.html`
- Features:
  - Date range pickers (top)
  - Top 5 issues summary (OpenAI-generated from selected date range)
  - Session count & message count stats
  - Harmonica sessions with full chat history (Q→A chronologically)
  - Only one harmonica open at a time
  - Sessions sorted chronologically
  - Combined label: "Chat History / # Messages:"

### **Airtable Integration**
- Table: `ChatLogs` (tblChatLogs) — Not yet created in Airtable
- Fields: sessionId, timestamp, date, ipAddress, browser, deviceType, messageType, message
- One row per message (separate question/answer rows)
- 10 fake chat sessions created for testing (52 total messages, one per day)

### **Security**
- ✅ OpenAI API key: **server-side only** (no exposure in client code)
- ✅ Google Maps API key: **server-side only** (moved from client)
- ✅ All external API calls go through secure PHP endpoints
- ✅ CORS headers restrict to agentrocketman.com

---

## Street View Integration — IN PROGRESS 🚧

Started building property Street View preview:

### **Files Created**
- `/api/maps-key.php` — Serves Google Maps API key from backend
- `/streetview.js` — Reusable Street View module
- `/index2.html` — New homepage with Street View (test version)
- `/service_routing2.html` — New service routing with Street View (test version)

### **Current Issue**
- Street View container renders but no imagery showing
- Getting "address not found" error even with autocomplete address
- Possible causes:
  1. Missing **Street View API** enable in Google Cloud (user was told to enable it)
  2. Address formatting issue (autocomplete not passing full formatted address)
  3. Street View coverage gaps in Toronto

### **Next Steps**
- Confirm user enabled "Street View API" (not just "Street View Static API")
- Debug address passing from autocomplete to geocoder
- Test with known Street View-covered addresses

---

## Other Updates

### **FAQ Reordering** ✅
- Sorted questions by customer journey (eligibility → details → operations → commitments → guarantees)
- 6 questions total (last added: bin rollout timing)

### **Domain Transfer Discussion**
- Recommended transferring getmybin.com from GoDaddy to Hostinger
- Consolidates infrastructure (one vendor, one dashboard)
- Reduces friction for DNS/email/SSL management

---

## Airtable Credentials (For Reference)
- Base ID: `apptYNRJTXwItvied`
- API Key: `patxbDkv88pOMXmYx.c7e5fd7974954e3a674087090835d11dd69504f3912f0ef86c3c59f1e91febdd`
- ChatLogs Table: `tblChatLogs` (or `tblatXRj8Ka7hyGyZ` — verify which exists)

---

## Commits This Session
1. FAQ reordering
2. Chat analytics system
3. Analytics fixes (title wrapping, chat history display)
4. OpenAI key to server-side
5. Rename analytics.html → chat_analytics.html
6. Street View integration (in progress)
