# 2026-06-20 - Bookings Tab & "Both" Orders Discussion

## Fixed: Bookings Tab Count Mismatch
- **Issue:** Bookings chart showed count (e.g., 3) but table showed fewer records
- **Root Cause:** Orders list filtered by Service Date only; didn't load orders from bookings created in date range but with service dates outside range
- **Fix:** Modified `/api/get-orders-list.php` to include:
  - Orders with Service Date in range
  - AND orders from bookings created in range (regardless of service date)
- **Result:** Chart count now matches table data

## Decision: Show ALL Bookings (Including Cancelled)
- **Changed:** Bookings tab now includes ALL bookings regardless of status
- **Previous:** Was excluding cancelled bookings
- **Bookings chart:** Shows total count of ALL bookings created on each day
- **Bookings filter:** Clicking date bar shows all orders from all bookings created that day

## Data Cleanup
- ✅ Deleted all 31 orders from Orders table
- ✅ Deleted all 13 bookings from Bookings table
- Both tables now empty for fresh test data

## "Both" Orders - Option B Confirmed
- **Requirement:** When user submits booking with Service Type = "both":
  1. Insert 1 record into Bookings table
  2. Insert 2 records into Orders table:
     - One with Service Type = "Roll Out"
     - One with Service Type = "Roll In"
     - Both linked to same Booking ID
     - Both with same Service Date
- **Implementation:** Uses same insertion method as existing Roll In/Roll Out orders
- **Discovery:** THIS IS ALREADY IMPLEMENTED in `/public_html/airtable-bookings.js`
  - Lines 153-154: `serviceJobs` array checks if svcType === 'both' and adds both job types
  - Lines 183-204: Loop creates separate order records for each job
  - Roll Out: `workDate = serviceDate - 1 day` (unless night zone)
  - Roll In: `workDate = serviceDate`

## No Code Changes Needed
The "both" splitting functionality already exists. When visitors submit a booking with Service Type = "both", the system automatically:
1. Creates 1 booking record
2. Creates 2 order records (Roll Out + Roll In)
3. Links both to same Booking ID with same Service Date
4. Applies correct work dates (Roll Out day before, Roll In same day)

**Ready to test:** Submit test booking with "both" service type → should see 1 booking + 2 orders in Airtable
