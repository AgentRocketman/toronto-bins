# 2026-07-15

## ElevenLabs Music v2 Integration — COMPLETE ✅

Chris said ElevenLabs has music generation. He was right — I was building static MP3 tracks with Google Lyria, but ElevenLabs has `POST /v1/music` with `music_v2` model.

**What changed:**
- Created `agentado/api/elevenlabs-music.php` — backend proxy to ElevenLabs Music API
  - Accepts `prompt`, `duration_ms`, `model_id`
  - `force_instrumental: true`, `output_format: mp3_48000_192`
  - MD5-based file caching in `output/music-cache/`
  - Same API key as TTS: `sk_1b300ca925e3778885f4a8595960423130fe660c3e2d5082`
- Frontend `MUSIC_TRACKS` replaced with `MUSIC_VIBES` — prompt templates instead of file paths
- `fetchMusic()` now calls `/agentado/api/elevenlabs-music.php` with prompt + duration matching voice-over
- Music selector updated with 5 vibes:
  1. 🎹 Elegant Piano — warm refined solo piano
  2. 🎸 Bright & Fresh — acoustic guitar, upbeat modern
  3. 🌿 Serene Ambient — floating pads, spa-like
  4. 🎬 Cinematic — sweeping strings, grand emotional
  5. 🎷 Smooth Jazz — sophisticated lounge, soft drums
- Music duration auto-matches voice-over length for perfect sync
- ~6.8s generation time per track, cached server-side
- Deployed to dev

## 3D AI Cinematic Video — RESEARCHED, AWAITING CHRIS

Chris wants a second generation mode: "🎬 3D AI Cinematic Video" using Kling 2.0 via Together AI.

**Research findings (Together AI Kling API):**
- Endpoint: `POST https://api.together.xyz/v2/videos`
- Model: `kwaivgI/kling-2.1-standard` ($0.18/video)
- Alternative: `kwaivgI/kling-2.0-master` (better quality, probably more expensive)
- Image-to-video: `frame_images: [{input_image, frame: 0}]` sets starting keyframe
- Resolution: 720p, 5 seconds per clip
- Flow: Create async job → poll status → get video_url when complete
- Generation time: ~3 min per clip (from Together AI docs example)

**Practical math (shared with Chris):**
- 10 photos × 3 min = ~30 min generation time
- 10 photos × $0.18 = ~$1.80 per property video
- 720p output (vs. 1080p Ken Burns)

**Proposed architecture:**
- Two buttons: "Standard" (Ken Burns, instant) and "🎬 3D Cinematic" (Kling, ~30 min)
- Server-side job queue: PHP backend sends all images to Kling, polls, downloads clips
- Two-phase progress bar: "Generating AI clips: 4/10" → "Compositing video: 42%"
- Canvas2D compositor: draws video clips with Ken Burns transforms, transitions, subtitles, audio
- Same toggles respected (music, voice, subtitles)

**Awaiting Chris on:**
1. Does he have a Together AI API key?
2. Is ~30 min generation time acceptable? (Can parallelize 2-3 clips to ~10 min)
3. Is 720p output ok?

This replaces the static-track Google Lyria approach for background music entirely.