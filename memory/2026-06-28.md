# 2026-06-28 — Memory Flush

## Mission Control — State Snapshot

### Builder Runner
- **Version:** v1.8-direct-deploy (last PID 48978)
- **Path:** `/data/.openclaw/workspace/builder-runner.py`
- **Long-polls:** `/api/runner-queue.php?wait=25`
- **Direct deploy:** tars `public_html`, calls `node /data/.openclaw/workspace/hostinger-deploy.mjs`, posts to `deploy.php` with internal_secret to mark deployed (~8–12s total)
- **Quick Fix:** Haiku 4.5, `--max-turns 15`, allowed tools include `Bash(git:*)`, no BUILDER_BRIEF.md/REQUIREMENTS.md, slim PATCH_INFO.md
- **Failure reports:** use `terminal_reason` + Claude output tail (no stale parent BUILDER_REPORT.md)
- **Success report:** only if mtime <10min

### Cost benchmarks (Quick Fix)
| Version | Tokens | Cost |
|---|---|---|
| v1.9 baseline | 92K | $1.38 |
| v1.15 (lean) | 702 | **$0.034** ⭐ |
| v1.17 (git+turns-15) | 1,656 | $0.065 |
| v1.18 (direct-deploy) | 1,834 | $0.074 |
| v1.19 | 5,534 | $0.106 |

→ **23× cheaper, 3.5× faster** than baseline.

### Disabled cron
- Job ID `1468a760-1425-4b84-a0b8-2579ee79d976` ("Deploy Queue Worker") — was burning ~28K tokens/30s polling empty queue. Saved ~$876/yr. Kept as failsafe (disabled in description).

### Dashboard fix
- `loadProjects()` catch handler no longer wipes grid on transient API failures.
- Keeps existing data, shows amber `#staleBanner` toast (3s). Full-screen error only on first-paint failure.

### Pipelines
- **Quick Fix:** `[builder, smoke_test, deploy]` (no Code Reviewer, all auto-approved)
- **Bug Fix:** `[builder, code_reviewer, smoke_test, deploy]`
- **Add Feature:** `[scout, architect, builder, code_reviewer, smoke_test, deploy]`

---

## OpenRouter / Kimi K2.7 Code — Session findings

### Status
- ✅ Wired into BOTH config layers (`/data/.openclaw/openclaw.json` gateway + `/data/.openclaw/agents/main/agent/models.json` agent registry)
- ✅ API key works, $10 credit funded
- ✅ Smoke test successful (~$0.001)
- ❌ **STILL BLOCKED via `session_status`** — rejected with `Model "openrouter/moonshotai/kimi-k2.7-code" is not allowed.`

### Allowlist enforcement (root cause investigation)
- File: `/usr/local/lib/node_modules/openclaw/dist/agent-command-DEmhTrQM.js`
- Logic: throws when `!allowAnyModel && !allowedModelKeys.has(explicitKey)`
- `allowAnyModel = !hasAllowlist`
- `hasAllowlist = Boolean(cfg.agents.defaults.models)` — **presence of `models` map creates implicit allowlist**
- Builder: `buildAllowedModelSet({ cfg, catalog, defaultProvider, defaultModel, agentId })` in `model-selection-CAAffjMN.js`
- Key normalization: `modelKey(provider, model)` in `model-selection-shared-BOD321LE.js`
- **Suspected cause:** key normalization mismatch between how `openrouter/moonshotai/kimi-k2.7-code` is stored vs. how the explicit key is parsed/normalized at lookup time. Need to inspect `normalizeModelRef`/`parseModelRef` to confirm.

### OpenRouter free tier — DEAD
All `:free` slugs return "use paid version instead":
- `deepseek/deepseek-chat-v3.1:free`
- `google/gemini-2.0-flash-exp:free`
- `meta-llama/llama-3.3-70b-instruct:free`
- `qwen/qwen-2.5-coder-32b-instruct:free`
- `moonshotai/kimi-k2:free`
- `z-ai/glm-4.5-air:free`
- `deepseek/deepseek-r1:free`
- `x-ai/grok-4-fast:free` (deprecated)

### Kimi K2.7 Code details
- Resolved slug: `moonshotai/kimi-k2.7-code-20260612`
- Reasoning model (15 reasoning tokens for trivial reply)
- Pricing: $0.50/M in, $2.00/M out (6× cheaper than Sonnet)
- Context: 200K
- Strong agentic/tool-use

### Gateway / agent config schemas (LEARNED)
**Gateway config (`openclaw.json`):**
- Provider `api` must be one of: `openai-completions | openai-responses | openai-codex-responses | anthropic-messages | google-generative-ai | github-copilot | bedrock-converse-stream | ollama | azure-openai-responses`
- OpenRouter uses `openai-completions` (NOT `openai-chat`)
- Each model needs `id` AND `name`

**Agent models.json (`/data/.openclaw/agents/main/agent/models.json`):**
- Per-model schema: `id`, `name`, `contextWindow`, `maxTokens`, `input: ["text"]`, `reasoning: bool`, `compat: {supportsDeveloperRole, supportsReasoningEffort, supportsStore}`, `cost: {cacheRead, cacheWrite, input, output}`

### Nexos billing proxy (DISCOVERED)
- Provider `nexos` + `nexos-anthropic` at `https://api.nexos.ai/v1`
- This is how existing Anthropic routing has been billed
- Token in agent models.json

---

## Backups created this session
- `/data/.openclaw/openclaw.json.bak-1782601183`
- `/data/.openclaw/agents/main/agent/models.json.bak-1782603141`

## Stale build dir warning
- `/data/.openclaw/workspace/builds/url-shortener-v1-16-fix/output/` — deleted "YESSSS" line, DO NOT use as parent for future copies.

---

## Next steps (carried from previous session)
1. **Debug Kimi allowlist rejection** — inspect `normalizeModelRef`/`parseModelRef` to find key mismatch. Likely fix: align how `openrouter/moonshotai/kimi-k2.7-code` is keyed in `cfg.agents.defaults.models` vs. catalog.
2. Once unblocked: side-by-side quality test vs Sonnet baseline.
3. If acceptable: set as `agents.defaults.model.primary` default.
4. **Reject → Iterate auto-loop** (~1-2 hrs, biggest remaining workflow paper-cut)
5. Wire real Telegram approval pings (chat ID `8714809782`)
6. Add Delete project button (~15 min QoL)
7. Real Add Feature test on fresh project (Bug Fix + Add Feature unproven E2E)
8. Budget hard-stop (~30 min)

## Critical creds/IDs (carry-forward)
- OpenRouter key: `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`
- Airtable token: `patxbDkv88pOMXmYx.c7e5fd7974954e3a674087090835d11dd69504f3912f0ef86c3c59f1e91febdd`
- Airtable base: `apptYNRJTXwItvied`
- Tables: Projects `tblZDjRO5OSIqzmEY`, AgentStatus `tblwlhJRTnuHzivlb`, Approvals `tblr4Wex6GwRwz4WE`
- Runner secret: `mc-runner-heartbeat-2026`
- Hostinger API token: `B4V2bxKyjkRgso0JS9CkiCqkqUZ32PhAzA16cxcB87d7b57e`
- Hostinger username: `u686706869`, domain `agentrocketman.com`
- MC password: `MissionControl2026!`
- URL Shortener parent: `recjMPAzE1CMk5inB` (deployed `/url-short/`)

---

## Mission Control — Reject → Iterate Auto-Loop (00:53 UTC)

**Requested:** Chris wanted the #1 open MC item — auto-loop on rejection.

**Implementation:**
- Created `public_html/mission-control/api/projects/patch-functions.php` with reusable `mcCreatePatchProject()`.
- Refactored `api/projects/patch.php` to use the helper (no endpoint contract change).
- Modified `api/approvals/decide.php`:
  - On reject with non-empty comments, auto-creates an iteration project.
  - Mode selection: `quick_fix` → quick_fix; `bug_fix`/`add_feature` → same; `full` → bug_fix.
  - Budget cap clamped to parent's remaining budget (default $3/$5).
  - Reject without comments still pauses; budget exhausted still pauses.
  - Double-reject guard preserved via existing `previousDecision !== 'pending'`.
- Updated `pipeline.html` + `dashboard.html`:
  - Rejection comment placeholders warn that comments are required for auto-iterate.
  - Toast shows created iteration label and first stage.

**Deployment:**
- Built deploy tar via `deploy-final.sh agentrocketman.com` (no bin-pics to back up).
- Deployed via `node hostinger-deploy.mjs agentrocketman.com /tmp/deploy-1782607986.tar.gz`.
- Verified live: `pipeline.html` contains the new placeholder text.

**Next:** Need real test by rejecting an approval with comments.

---

## Mission Control — Delete Button + Patch Archiving (01:13 UTC)

**Requested:** Delete project button + hide old patches behind a "Patches" link.

**Implementation:**
- Created `public_html/mission-control/api/projects/delete.php`:
  - Accepts POST `{project_id}`.
  - Loads project by ID via `airtableRequest('GET', table, [], id)`.
  - Deletes project record via `airtableRequest('DELETE', table, [], id)`.
  - Cleans up matching approval records.
  - **Fix applied after Claude generation:** corrected `airtableRequest` calls from concatenated path string to proper `(method, table, [], recordId)` signature.
- Updated `dashboard.html`:
  - Hover-delete button (`×`) on each project card with browser `confirm()`.
  - Default grid filters out patch projects (patch_mode set and parent exists).
  - Added "Show patches" checkbox next to grid header.
  - Root cards show "🌿 Patches (N)" badge; opens modal with mini patch cards.
  - Clicking a patch mini-card opens its detail modal.

**Deployment:**
- `deploy-final.sh agentrocketman.com` + `node hostinger-deploy.mjs agentrocketman.com /tmp/deploy-1782609209.tar.gz`.
- Verified live HTML contains new delete/patches elements.

**Next:** Test deleting a real project + verifying patch archive.

---

## Mission Control — Inline Patch Expansion (01:27 UTC)

**Requested:** Remove "Show patches" checkbox; expand patches inline under the parent project when clicking "Patches (N)" badge.

**Implementation:**
- Updated `public_html/mission-control/dashboard.html`:
  - Removed the "Show patches" checkbox from header and `showPatches` state.
  - Root project grid now always hides patch projects.
  - Added `.inline-patches-list` container inside root cards, hidden by default.
  - "🌿 Patches (N)" badge now toggles the inline container accordion-style.
  - Inline patch mini-cards show name, iteration label, mode badge, status, budget.
  - Clicking mini-card opens its detail modal.
  - Updated root-card click handler to ignore inline patch area.
  - Old `patchesListModal` hidden (left in DOM).

**Deployment:**
- `deploy-final.sh agentrocketman.com` + `node hostinger-deploy.mjs agentrocketman.com /tmp/deploy-1782610022.tar.gz`.
- Verified live styles present.

---

## Mission Control — Fix Inline Patch Accordion Collapsing on Refresh (01:33 UTC)

**Bug:** Expanding inline patches accordion collapsed after ~1s because `renderProjects()` rebuilds the grid every 2s and lost the open state.

**Fix:**
- Added `expandedPatches: {}` to dashboard state.
- Badge click toggles class and records parentId in `state.expandedPatches`.
- After each card render, if `state.expandedPatches[p.id]` is true, re-add `.open` to the inline list.

**Deployment:** `deploy-final.sh agentrocketman.com` + hostinger-deploy.mjs.

---

## Mission Control — Active Patches Show as Cards (01:44 UTC)

**Requested:** Active/running quick_fix patches should appear as top-level cards with progress, not only archived.

**Fix:**
- Updated `renderProjects()` filter in `dashboard.html`:
  - Root projects always show.
  - Patch projects show unless terminal: `status` is `complete`, `failed`, `paused`, or `deploy_status === 'deployed'`.
  - Finished/old patches remain hidden and accessible via parent card “Patches (N)” accordion.

**Deployment:** Same backup/deploy/restore workflow to agentrocketman.com.

---

## Mission Control — Sort Patches Descending (01:46 UTC)

**Requested:** Latest patch should be on top in the inline patches list.

**Fix:** Changed inline patches sort from ascending `la.localeCompare(lb)` to descending `lb.localeCompare(la)`.

**Deployment:** Same backup/deploy/restore workflow.

---

## Mission Control — Numeric Patch Sort Fix (01:48 UTC)

**Bug:** String sorting placed `v1.2` incorrectly relative to `v1.20` (lexicographic order).

**Fix:** Replaced `localeCompare` with numeric segment sort on iteration label parts, descending. Label “v1.20” now correctly sorts above “v1.2”.

**Deployment:** backup/deploy to agentrocketman.com.

---

## Mission Control — Sort Patches by Created Time Descending (01:49 UTC)

**Requested:** Sort patches by creation date descending (newest first).

**Fix:** Inline patches list now sorts by Airtable `createdTime` descending using string localeCompare.

**Deployment:** backup/deploy to agentrocketman.com.

---

## Mission Control — Group Active Patches Under Parent (01:53 UTC)

**Requested:** Active patch card should sit between parent project and other projects; when done, move into inline archived list.

**Fix:**
- Rewrote `renderProjects()` ordering:
  - Separated root projects and active patches.
  - Sorted each group by `createdTime` descending.
  - Built display order: each root followed by its active patch children; orphaned active patches appended at the end.
  - Terminal patches remain hidden from grid and appear only in the parent’s inline “Patches” list.

**Deployment:** backup/deploy to agentrocketman.com.

---

## Mission Control — Pipeline Queue + Run Jobs (02:18 UTC)

**Requested:**
- Pipeline page should show in-progress / queued jobs, not completed/deployed jobs.
- Add ability to queue a patch from the Dashboard quick-patch modal.
- Add a “Run Queued Jobs” button on Pipeline that runs jobs sequentially, and a manual “Run” button on each queued job card.
- If one job fails, continue to the next queued job.

**Implementation:**
- Backend `api/projects/patch-functions.php`:
  - `mcCreatePatchProject()` now accepts a 6th parameter `$queue = false`.
  - When `$queue` is true, the child project is PATCHed with `current_stage='queued'` and `current_agent='Queued'`; no first-stage fields are patched and `run.php` is not triggered.
- Backend `api/projects/patch.php`:
  - Reads `queue` from the JSON body and passes it through.
- Frontend `dashboard.html`:
  - Patch modal now has two buttons: **Start Patch** (existing) and **Queue Patch**.
  - `submitPatch(startNow=true)` sends `queue:true` when `startNow` is false.
  - Queue success toasts “📥 patch queued — find it in Pipeline.”
- Frontend `pipeline.html`:
  - Removed the Deployed board column; completed/deployed jobs stay in Dashboard.
  - Added a **📥 Queue** column on the far left.
  - Added a header **▶ Run Queued Jobs** toggle button (persists on/off in `localStorage` key `mcQueueRunnerActive`).
  - Queued cards show a **▶ Run This Job** manual-start button, guarded against click-to-open-modal.
  - Added queue runner logic:
    - `runNextQueued()` starts the oldest queued project (by `createdTime`).
    - `maybeContinueQueue()` runs after each poll; it only starts the next job when no project is currently active (not queued and not terminal). A job that fails is treated as terminal, so the runner moves on.
    - Manual card clicks start that job without triggering the auto-runner; the auto-runner still respects the active-job gate.
  - Updated `STAGES` to lead with `'queued'` and replaced `'code_auditor'` with `'code_reviewer'` to match the visible Code Reviewer column.

**Philosophy notes:**
- `current_stage = 'queued'` is a plain-text placeholder; `status` singleSelect is left untouched so Airtable schema changes aren’t needed.
- The auto-runner intentionally gates on *any* active project, not just the one it started, so it won’t race with a manually-started job.

**Deployment:**
- `bash /data/.openclaw/workspace/deploy-final.sh agentrocketman.com`
- `node /data/.openclaw/workspace/hostinger-deploy.mjs agentrocketman.com /tmp/deploy-*.tar.gz`

---

## Mission Control — UI Polish + Queue Runner Diagnostics (03:31 UTC)

**Requested:**
- Stop the Pipeline refresh indicator text from resizing and pushing elements around.
- Show how long each archived patch took in the inline patch list.
- Investigate a queued job failing when auto-started but succeeding when manually started.

**Implementation:**
- `pipeline.html`:
  - Replaced variable-length refresh text with a fixed clock: `Live · <span class="tab-nums">HH:MM:SS</span>`.
  - Added `tabular-nums` + fixed `min-width` CSS so only the time value changes each poll; no layout shift.
- `dashboard.html`:
  - Added `formatDuration(ms)` and `computePatchDurationMs(patch)` helpers.
  - Inline archived patch mini-cards now show e.g. `⏱ took 1m 23s`.
  - Duration measured from patch `createdTime` to the latest terminal agent-status event (`complete/failed/deployed/paused`), falling back to now if no event found.
- `builder-runner.py`:
  - `start_project()` now logs the successful response so we can see which stage was triggered.
  - Non-2xx responses and JSON errors are raised/logged as failures.
  - `process_queue()` now logs when it skips because a job is already in progress (and which one) and when no queued jobs exist.
  - Restarted the daemon after the update.

**Deployment:** `deploy-final.sh` + `hostinger-deploy.mjs` to agentrocketman.com; restarted Builder Runner daemon.
