# 2026-07-06

## Voice-to-agent exploration
- Chris wants to speak with *me* (the OpenClaw session), not a separate chatbot.
- Explored OpenClaw's native `voice-call` plugin.
- Installed `@openclaw/voice-call@2026.5.6`; it loaded but `voicecall.start` RPC was unknown.
- Started updating OpenClaw to `2026.6.11` to fix compatibility.
- Chris stopped me mid-update: he wants to speak with me, not an LLM.
- Killed the update process (`nimble-prairie`). Gateway still on `2026.5.6`.
- Voice-call plugin remains installed/enabled in config, but unused.

## Options discussed for actually speaking with me
1. Telegram voice notes here — actually me, free, already works.
2. Voicemail + callback phone bridge — actually me, but async; call in, I call back.
3. Live delayed phone bridge — actually me, but 5–20s delay per turn.
4. Chatbot/LLM voice assistant — rejected by Chris.

## OpenClaw update notes
- `openclaw update status` shows `2026.6.11` available.
- Update command: `openclaw update --tag 2026.6.11 --yes`.
- Dry-run looked safe; actual update was aborted before changes.

## PWA build (voice messaging with me)
- Built PWA at `/public_html/assistant/`:
  - `index.html`, `style.css`, `app.js`, `manifest.json`, `sw.js`
  - Backend: `api/send.php`, `api/poll.php`, `api/media.php`, `api/config.php` (placeholder)
- Press-and-hold mic records audio, sends to backend.
- Backend will forward to a dedicated Telegram bot, I'll reply via Telegram, backend polls and plays reply.
- Deployed to https://agentrocketman.com/assistant/.
- Backend token placeholder is `__SET_ME__`; waiting for Chris to create a Telegram bot via @BotFather and provide the token.
- Once token received, will add new Telegram channel to OpenClaw config and restart gateway.

## Next step
- Receive Telegram bot token from Chris, configure, restart gateway, test end-to-end.
